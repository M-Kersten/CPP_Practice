//#include om libraries in te laden, i(input)o(output)stream(van data) om data in en uit te laden via het console
#include <iostream>
// std staat voor standaard
using namespace std;

int main() 
{
    //c (taal) out(output)
    // << staat voor het uitvoeren van data
    cout << "Hello, World!";
    // c in(input) wachten tot de gebruiker input geeft
    cin.get();
    // het programma sluiten
    return 0;
}
